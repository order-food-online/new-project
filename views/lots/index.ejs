<% include ../partials/header %>
	
	<header class="jumbotron">
		<div class="container">
			<h1>Welcome To Have Lots Need Lots</h1>
			<p>View Active Listings in Calgary, Area!</p>
			<p><a class="btn btn-primary btn-large" href="/lots/new">Add New Listing</a></p>
		</div>
	</header>

	<div class="row">
		<div class="col-lg-12">
			<h3>
				Our Active Listings
			</h3>
		</div>
	</div>
	
    <div class="row text-center" style="display:flex; flex-wrap: wrap;">
        <% lots.forEach(function(lots){ %>
        <div class="col-md-3 col-sm-6">
            <div class="thumbnail">
                <img src="<%= lots.image.url %>" class="img-responsive" style="height:250px; width: 300px;">
                <div class="caption">
                    <h4><%= lots.name %></h4>
                    <% if (lots.rating === 0) { %>
                    <em>No reviews yet.</em>
                    <% } else { %>
                    <span class="fa fa-star checked"></span>
                    <span class="fa fa-star <% if (lots.rating > 1.5) { %> checked <% } %>"></span>
                    <span class="fa fa-star <% if (lots.rating > 2.5) { %> checked <% } %>"></span>
                    <span class="fa fa-star <% if (lots.rating > 3.5) { %> checked <% } %>"></span>
                    <span class="fa fa-star <% if (lots.rating > 4.5) { %> checked <% } %>"></span>
                    <% } %>
                </div>
				<div class="caption">
				<span class="badge label-primary"><i class="fa fa-thumbs-up"></i> <%= lots.likes.length %></span>
				</div>
                <p>
                    <a href="/lots/<%= lots._id %>" class="btn btn-primary btn-md active"><i class="fa fa-plus"></i> View Listing</a>
                </p>
            </div>
        </div>
        <% }); %>
    </div>
</div>

<% include ../partials/footer %>